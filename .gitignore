# .gitignore en la raíz del proyecto CRM

# Python (Backend)
backend/env/ # Entorno virtual de Python
backend/__pycache__/
backend/*.pyc
backend/db.sqlite3 # Base de datos SQLite local
backend/.env # Variables de entorno (si las tienes)
backend/media/ # Archivos subidos por usuarios (si los hay)
backend/static/ # Archivos estáticos recolectados (si los hay)

# Node (Frontend)
frontend/node_modules/ # Dependencias de Node
frontend/dist/ # Archivos de build de Vite
frontend/.env # Variables de entorno (si las tienes)

# General
.DS_Store # macOS
.vscode/ # Configuración de VS Code (opcional, si no quieres compartirla)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# logs
*.log